# Python dependencies for the asset/document generators in this directory.
# These are BUILD-TIME tooling, not runtime — `npm run build` never invokes
# them and a deployment does not need Python at all.
#
#   python -m venv .venv
#   .venv/Scripts/activate      # Windows;  source .venv/bin/activate elsewhere
#   pip install -r scripts/requirements.txt
#
# Python 3.10+ (vtracer ships as a Rust wheel; on an unsupported interpreter
# pip falls back to building from source and needs a Rust toolchain).

python-pptx>=0.6.23     # generate_docs.py  — feasibility deck
python-docx>=1.1.0      # generate_docs.py  — feasibility study
Pillow>=10.0.0          # make_social_assets.py, vectorize_gifs.py
vtracer>=0.6.11         # vectorize_gifs.py  — GIF -> SVG auto-trace
